import 'package:multi_restaurant_app/package_routes.dart';

class AppTheme {
  static const Color primaryColor = Color.fromARGB(255, 255, 255, 255);
  static const Color secondaryColor = Color(0xFFFDB730);
  static const Color titleColor = Color(0xFF0654ba);
  static const bText = Colors.black;

  // * icon color
  static const wIcon = Colors.white;
  static const bIcon = Colors.black;
  static const gIcon = Color(0xFFFDB730);
}
